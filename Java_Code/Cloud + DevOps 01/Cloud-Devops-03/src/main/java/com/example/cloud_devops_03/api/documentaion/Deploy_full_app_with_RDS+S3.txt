STEP 1 — Create AWS Account

Go to:

AWS Console

Create:

AWS account
Billing setup
IAM admin user (DON’T use root account daily)

Learn:

Region selection (ap-south-1 for India)
IAM users
Security basics
STEP 2 — Create Your Database (RDS)

Go to:

Amazon RDS

Choose:

PostgreSQL OR MySQL
Free tier initially

Recommended:

PostgreSQL
Important Settings
Setting	Value
Engine	PostgreSQL
Public Access	No
Multi-AZ	Off initially
DB Instance	db.t3.micro
Storage	20GB

Create:

DB username
Strong password

After creation you get:

DATABASE_URL=postgresql://user:password@host:5432/dbname
0 → 10
DB works
10 → 100
Multi-AZ
Automated backups
Read replicas
Monitoring



STEP 3 — Create S3 Bucket

Go to:

Amazon S3

Create bucket:

Unique bucket name
Same region as app

Example:

myapp-prod-uploads
Configure

Enable:

Versioning
Block public access (initially)

Use for:

Images
PDFs
Videos
Static files
Backups
Backend Upload Flow
User uploads file
→ Backend receives
→ Uploads to S3
→ Saves URL in DB
100-level

Use:

Presigned URLs
CloudFront CDN
Lifecycle rules


STEP 4 — Launch Backend Server

Simplest options:

Option	Good For
EC2	Learning
ECS/Fargate	Production
Elastic Beanstalk	Easy deployment
Docker on VPS	Cheap/simple

For beginners:
Use EC2.

Go to:

Amazon EC2

Launch:

Ubuntu Server
t2.micro or t3.micro

Open ports:

22 SSH
80 HTTP
443 HTTPS


STEP 5 — Deploy Backend Code

SSH into server:

ssh -i key.pem ubuntu@SERVER_IP

Install:

sudo apt update
sudo apt install nodejs npm nginx git

Clone app:

git clone YOUR_REPO
cd app
npm install

Add environment variables:

DATABASE_URL=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
S3_BUCKET=
JWT_SECRET=

Run:

npm start

Production:
Use PM2.

npm install -g pm2
pm2 start server.js
pm2 save


STEP 6 — Connect Backend to RDS

Inside backend config:

const db = new Pool({
 connectionString: process.env.DATABASE_URL
})

Test:

npm run migrate

Check:

Tables created
Backend can read/write
Common Issue

RDS Security Group must allow EC2 access.

Add inbound rule:

PostgreSQL 5432
Source = EC2 security group

STEP 7 — Connect Backend to S3

Install SDK:

npm install @aws-sdk/client-s3

Example upload:

const s3 = new S3Client({
 region: "ap-south-1"
})

Upload:

await s3.send(new PutObjectCommand({
 Bucket: process.env.S3_BUCKET,
 Key: fileName,
 Body: fileBuffer
}))

Save URL into database.



STEP 8 — Deploy Frontend

Options:

Frontend	Best Platform
React/Vite	S3 + CloudFront
Next.js	Vercel
Static site	S3 Hosting

Easy path:
Use Vercel.

Go to:

Vercel

Connect GitHub repo.

Add API URL:

VITE_API_URL=https://api.myapp.com

Deploy.

